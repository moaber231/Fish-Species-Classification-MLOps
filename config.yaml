# config.yaml
workerPoolSpecs:
  machineSpec:
    machineType: n1-standard-4 # A balanced machine with 4 vCPUs and 15GB RAM
  replicaCount: 1 # Single machine training
  containerSpec:
    # Use the EXACT path from your successful build
    imageUri: europe-west1-docker.pkg.dev/fish-mlops/fish/train:native
